import { View, Text } from 'react-native'
import React from 'react'

const ExpenseItemCard = ({item}) => {
  return (
    <View>
      <Text>ExpenseItemCard</Text>
    </View>
  )
}

export default ExpenseItemCard